__pycache__/
*.pyc
*.pyo
Athaliana_167_TAIR9.fa
*.nbc
/build
.DS_Store
*.prof
*.so
*.html
*.dat
*.mask
.vscode
/ULTRA
slurm-*.out
/ath_cen/
/.claude/
/.codex
*.settings
benchmarking/TideHunter-v1.5.6_arm64-macos/
.vscode

# Session handoff note — working memo, rewritten each session, not project history
resume.md
resume.md.*
resume-archive-*.md

# Manuscript working backups — one per revision pass, not project history
manuscript.md.*-bak

# Build residue from `pip install .` / `python -m build`
*.egg-info/
build/
dist/

# Cython-generated C (regenerated by any build; source of truth is the .pyx)
bwtandem/_accelerators.c

# Scorer scratch (score_table1.py creates and removes it; a crashed run leaves it behind)
scripts/scoring/work/
